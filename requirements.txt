# Existing dependencies
jira==3.5.2
python-dotenv==1.0.0
rich==13.7.0
tabulate==0.9.0

# RAG Brain dependencies (100% local, no cloud AI)
httpx==0.27.0           # HTTP client for Ollama
numpy==1.26.4           # Vector operations
rank-bm25==0.2.2        # BM25 sparse retrieval
pyyaml==6.0.1           # YAML parsing for knowledge base
