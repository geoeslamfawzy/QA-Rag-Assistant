#!/usr/bin/env bash
# Run: ./bin/review <story_key>
# Example: ./bin/review CMB-32860
# Output: output/review/review-{KEY}.md
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$SCRIPT_DIR"
exec "$SCRIPT_DIR/venv/bin/python3" main.py review "$@"
